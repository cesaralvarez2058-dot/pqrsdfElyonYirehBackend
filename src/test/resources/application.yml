spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pqrsdfelyonyireh
    username: postgres
    password: Csr73203440**
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080

# Configuración para subida de archivos grandes (evidencias)
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 15MB
